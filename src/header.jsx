import React from 'react';
import ReactDOM from 'react-dom';

class Header extends React.Component {
    render() {
        return (
            <section id="header">
                <div class="header-login pull-right">Hey!</div>
            </section>
        );
    }
}

module.exports = Header;
